---
title: 3D
description: Функции 3D отрисовки
sidebar_position: 4
---

# 3D

# Функции трёхмерной отрисовки

## Mantle.func.draw_ent_text

Рисует текст на сущностью.

![](./assets/3d-1.gif)

```js
hook.Add('HUDPaint', 'Test', function()
    for _, ent in ipairs(ents.FindByClass('prop_physics')) do
        if !IsValid(ent) then continue end

        Mantle.func.draw_ent_text(ent, ent:GetModel():GetFileFromFilename(), 60)
    end
end)
```

| Аргумент | Тип      | Описание                                           |
| -------- | -------- | -------------------------------------------------- |
| `ent`    | `object` | Сущность, над которой рисуется текст               |
| `text`   | `string` | Отображаемый текст                                 |
| `posY`   | `int`    | Смещение по вертикали относительно центра сущности |
